# Rules

## Git Workflows
- **Always Commit and Push**: Whenever you edit files, add new files, or make modifications to the codebase, always make sure to commit the changes and push them to the current git branch remote (`git push`) to keep the repository updated. Use descriptive commit messages.
