# Rules

## Git Workflows
- **Always Commit and Push**: Whenever you edit files, add new files, or make modifications to the codebase, always make sure to commit the changes and push them to the upstream remote branch `final` (`git push upstream final`) to keep the repository updated. Use descriptive commit messages.
